# Web-Application-Firewall
This project implements a basic Web Application Firewall (WAF) using Flask. It monitors incoming HTTP requests and detects common attacks like SQL Injection and Cross-Site Scripting (XSS). The system filters malicious inputs, logs request activity, and applies rate limiting to enhance web application security.
